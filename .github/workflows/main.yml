# This is a basic workflow to help you get started with Actions

# ------------------------------------ #
#       Made By An V.1.0.0           #
# ------------------------------------ #
name: My Python Web
# ------------------------------------ #
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
# ------------------------------------ #
jobs:
  bkjs:
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        testi: [1.1, 27, 278, 2v, 9e, 93h, os, yys,]
        flaga: [Aqw, his, ]
    steps:
    - name: Setup
      uses: actions/checkout@v1
    - name: runneyr_go
      run: |
        sudo apt update -y && sleep 5
        
  ipsjrt-linux:
    needs: [bkjs]
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: [3.9]
        check: [tes, his, uos, 7sh, hjs, u9s, hhus, uis, yihs, 67a, uis, 73j, hks9]
    steps:
    - name: Checkout Code
      uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: EBroo maov .
      run: |
        cd stc
        ls
        pip install selenium
        pip install names
        python gio.py
  ipsjrt-mac:
    needs: [bkjs]
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: [3.8]
        check: [madt, 11, 22, 33, 44, 55, 58, 82, 7s, hi8, hs2, 79s, hja]
    steps:
    - name: Checkout Code
      uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: EBroo mao ..
      run: |
        cd stc
        ls
        pip install selenium
        pip install names
        python gio.py
